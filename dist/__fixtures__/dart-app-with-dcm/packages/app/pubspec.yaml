name: app
description: A simple app that uses the core library
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  core:
    path: ../core

dev_dependencies:
  lints: ^4.0.0
